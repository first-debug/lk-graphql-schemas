type UserAuthResult {
  email: String!
  # TODO: найти более безопасный способ предоставлять хеш пароля
  passwordHash: String!
  role: String
  version: Int
}

type Query {
  userAuth(email: String!): UserAuthResult
}
